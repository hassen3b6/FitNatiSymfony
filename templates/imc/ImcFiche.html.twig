<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link href="https://fonts.googleapis.com/css?family=Poppins:100,100i,200,200i,300,300i,400,400i,500,500i,600,600i,700,700i,800,800i,900,900i&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css">
    <meta http-equiv="Content-Security-Policy" content="script-src https://www.google.com/recaptcha/ https://www.gstatic.com/recaptcha/; frame-src https://www.google.com/recaptcha/ https://recaptcha.google.com/recaptcha/">
    <title>Fitnatic HOME PAGE</title>
    <link rel="stylesheet" type="text/css" href="{{ asset('CalculImc/Style.css') }}">
    <style>
        /* Add your custom styles here */
  main {
    display: flex;
    justify-content: flex-end; /* Align to the right */
    align-items: center;
    height: 100vh;
}


        h2 {
            color: #333;
        }

        p {
            color: #555;
        }
/* Style for the form grid */
.form-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--gap);
    max-width: 600px;
    margin: 0 auto;
}

/* Style for the input wrapper */
.input-wrapper {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--gap);
    align-items: center;
}

/* Style for the gender input wrapper */
.gender-input-wrapper {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--gap);
    align-items: left;
}


        .custom-input:focus {
            outline: none;
            border-color: #ffc107; /* Change to your desired focus color */
        }

        .recaptcha-wrapper {
            display: flex;
            flex-direction: column;
            align-items: center;
            length:50%;
            width:100%;
        }

        #calculerBtn {
            background-color: #ffc107; /* Change to your desired button color */
            color: #333;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        #calculerBtn:hover {
            background-color: #333; /* Change to your desired hover color */
            color: #ffc107;
        }

        /* Result Styles */
        h3 {
            color: #333;
        }

        span {
            color: #555;
        }

        p {
            color: #777;
        }
    </style>
</head>

<body class="light-theme">
    <!-- Header -->
    <header class="header" id="header">
        <nav class="navbar container">
            <a href="#">
                <h2 class="nav-brand">Fit Natic</h2>
            </a>
            <div class="menu" id="menu">
                <ul class="list">
                    <li class="list-item">
                        <a href="{{ path('add_Fiche') }}" class="list-link">Calcul IMC</a>
                    </li>
                    <li class="list-item screen-lg-">
                        <a href="{{ path('calculate_calorique') }}">Besoins Caloriques </a>
                    </li>
                    <li class="list-item screen-lg-">
                        <a href="#" class="list-link">Regimes Alimentaires </a>
                    </li>
                
                </ul>
            </div>
            <div class="list list-right">
           <button class="btn place-items-center" id="theme-toggle-btn">
    <i class="ri-sun-line sun-icon"></i>
    <i class="ri-moon-line moon-icon"></i>
</button>

                <button class="btn place-items-center screen-lg-hidden menu-toggle-icon" id="menu-toggle-btn">
                    <i class="ri-menu-3-line open-icon"></i>
                    <i class="ri-close-line close-icon"></i>
                </button>
                <a href="{{ path('afficher_Imcs') }}" class="list-link screen-sm-hidden btn sign-in-btn">Fiche Personnelles</a>
            </div>
        </nav>
    </header>
    <!-- Search -->
    <main class="d-grid">
        <!-- Tricks-and-tips-articles -->
        <section class="tricks-and-tips-articles section-header-offset text-center" id="tricks-and-tips">
            <h2>Calculateur d'IMC</h2>
            <p>L'Indice de Masse Corporelle (IMC) est une mesure de la corpulence d'une personne. Il est calculé en fonction du poids et de la taille. Utilisez le formulaire ci-dessous pour obtenir votre IMC.</p>
            <br>
            <br>
            {# Render the Symfony form #}
            {{ form_start(form) }}

<div class="form-grid">

  <div class="gender-input-wrapper">
   
        {{ form_row(form.sexe, {'attr': {'class': 'custom-input gender-input'}}) }}
        <span>{{ form_errors(form.sexe) }}</span>
    </div>
    <div class="input-wrapper">
     
        {{ form_row(form.age, {'attr': {'class': 'custom-input age-input'}}) }}
        <span>{{ form_errors(form.age) }}</span>

    </div>
        <div class="input-wrapper">
     
        {{ form_row(form.taille, {'attr': {'class': 'custom-input age-input'}}) }}
        <span>{{ form_errors(form.taille) }}</span>
        
    </div>
        <div class="input-wrapper">
     
        {{ form_row(form.poids, {'attr': {'class': 'custom-input age-input'}}) }}
        <span>{{ form_errors(form.poids) }}</span>
        
    </div>
  
    <!-- Add other input wrappers for weight, height, and reCAPTCHA as needed -->
</div>
<div class="recaptcha-wrapper">
    {{ form_row(form.recaptcha) }}
    <span>{{ form_errors(form.recaptcha) }}</span>
</div>
<br>
<div>
    <button type="submit" class="btn btn-warning mt-3" id="calculerBtn">Calculer</button>
</div>
{{ form_end(form) }}

            <div>
                {% if imcResult %}
                <div>
                    <h3>Resultat IMC : </h3>
                    <span>IMC: {{ imcResult.imc }}</span>
                    <br>
                    <span>Catégorie IMC: {{ imcResult.categorie }}</span>
                    <br>
                    <span>Poids Idéal: {{ imcResult.poidsIdeal }}</span>
                    {% if imcResult.categorie == 'Obésité' %}
                    <p>Attention! Vous êtes dans la catégorie d'obésité. Veuillez consulter un professionnel de la santé.</p>
                    {% elseif imcResult.categorie == 'Surpoids' %}
                    <p>Vous êtes en surpoids. Il est recommandé de maintenir un mode de vie sain.</p>
                    {% elseif imcResult.categorie == 'Sous-poids' %}
                    <p>Vous êtes en sous-poids. Assurez-vous de maintenir une alimentation équilibrée.</p>
                    {% elseif imcResult.categorie == 'Poids normal' %}
                    <p>Votre poids est normal. Continuez à adopter de bonnes habitudes alimentaires et de vie.</p>
                    {% endif %}
                </div>
                {% endif %}
            </div>
        </section>
    </main>
    <script src="{{ asset('CalculImc/imc.js') }}"></script>
    <script src="https://www.google.com/recaptcha/api.js?render=6LezLRwpAAAAAEjMP2ioowU3K7cjkZxxr6ajrDS7"></script>
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
</body>

</html>
