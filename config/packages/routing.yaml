framework:
    router:
        utf8: true

        # Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
        # See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
        # default_uri: http://localhost

    # Configure other framework options if needed
    # ...

# Add your production-specific configuration
# Note: This will override the settings from the main configuration when in the "prod" environment.
# You can customize it based on your production requirements.
# when@prod:
#     framework:
#         router:
#             strict_requirements: null
